import React, { Component } from 'react';
import { connect } from 'react-redux';


class List extends Component {
    render() {
        return (
            <div onClick={ this.props.clcik_add_state.bind(this,'hehe') }>{ this.props.abc }</div>
        )
    }
}
const a = (state) =>{
    return {
        abc:state.List.abc
    }
}

const b = (dispatch) => {
    return {
        clcik_add_state(n,e) {






















































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































            const action = {
                abc:2222,
                type:'clcik_add_state',
                n
            }
            dispatch(action);
        }
    }
}

export default connect(a, b)(List);